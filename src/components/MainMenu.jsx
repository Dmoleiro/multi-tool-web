import React from 'react';
import styles from '../styles/MainMenu.module.css';

function MainMenu() {
    return (
        <div className={styles.buttonsContainer}>Choose a tool from the left menu</div>
    );
}

export default MainMenu;